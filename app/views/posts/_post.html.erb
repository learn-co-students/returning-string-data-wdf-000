<h1><%= post.id %>: <%= post.title %></h2>
<div id="body-<%= post.id %>"><%= truncate post.description %></div>

<%# This button will triggger ajax call to our body route %>
<%# prefixing the class name with 'js-' denotes this will be used as a JS selector %>
<%# using the data-* attribute allows us to pass information we need to use in our JS %>
<button class="js-more" data-id="<%= post.id %>">More...</button>
